/// <reference types="Cypress" />
import Reports from '../../support/pageObjects/Reports'
const report = new Reports()
const Constvarbl = require('../../fixtures/ConstVarbl.json')
describe('My First Test Suite', function() 
{
    it('Signin', ()=>{
        cy.Signin(Constvarbl.TeacherUserName,
            Constvarbl.TeacherPassword);
     }); 
   it('Teacher Student Reports',function() {
        cy.Curriculum()
        report.getStudentReports()
            .click({force:true})
        report.getClass()
            .contains(Constvarbl.Class)
        report.getName()
            .type(Constvarbl.Organisation)
        report.getSection()
            .contains(Constvarbl.Section)
        report.getPerformance()
            .click()
        report.getMonthPerformance()
            .contains(Constvarbl.MonthPerform) 
        report.getSubjects()
            .contains(Constvarbl.GradeSubj)
        report.getChapters()
            .contains(Constvarbl.gradechapter)
        cy.wait(1000)    
        report.getPerformTopic()
            .contains(Constvarbl.PerformTopic)
                .click({force:true})
        report.getActivityTab()
            .click()
        report.getAssessTab()
            .click()
        report.getAssignTab()
            .click()
    })
    it('Teacher Usage Reports ',function() {  
        report.getStudentReports()
            .click({force:true})
        report.getClass()
            .contains(Constvarbl.Class)
        report.getName()
            .type(Constvarbl.Organisation)
        report.getSection()
            .contains(Constvarbl.Section)
        report.getUasgeReport()
            .click()
        report.getMonthUsage()
            .contains(Constvarbl.MonthUsage) 
        report.getSubjects()
            .contains(Constvarbl.GradeSubj)
        report.getActivityTab()
            .click()
        report.getAssessTab()
            .click()
        report.getAssignTab()
            .click()
        report.getCollaborationTab()
            .click({force: true})
    })
    it('Teacher Comparative Reports ',function() {
        report.getStudentReports()
            .click({force:true})
        report.getClass()
            .contains(Constvarbl.Class)
        report.getName()
            .type(Constvarbl.Organisation)
        report.getSection()
            .contains(Constvarbl.Section)
        report.getComparativeperf()
            .click()
        report.getComparativesubjct()
            .contains(Constvarbl.Subjects)
        report.getComparativeChapter()
            .contains(Constvarbl.SubjIntro)
        cy.wait(1000)    
        report.getComparativetopics()
            .contains(Constvarbl.PerformTopic)
                .click({force:true})
        report.getComparativegrade()
            .contains(Constvarbl.grade)
        report.getCustomdates()
            .contains(Constvarbl.CustomDate)
        report.getActivityTab()
            .click()
        report.getAssessTab()
            .click()
        report.getAssignTab()
            .click()    
    })  
    it('Teacher Class Reports ',function() {
        report.getClsReports()
            .click({force:true})
        report.getClsreportGrade()
            .contains(Constvarbl.Class)
        report.getClasSubjct()
            .contains(Constvarbl.PScinece)
        report.getClasSection()
            .contains(Constvarbl.Section)
        report.getActivityTab()
            .click()
        report.getAssessTab()
            .click()
        report.getAssignTab()
            .click()
        cy.Logout()
    })    
    
})    